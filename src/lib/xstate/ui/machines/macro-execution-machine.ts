/**
 * @fileoverview Macro Execution Machine - UI Integration Stub
 * 
 * This is a stub file to resolve TypeScript compilation errors.
 * It provides minimal types needed for UI integration without
 * full XState machine implementation.
 */

export interface MacroExecutionMachine {
  id: string;
  type: 'macro-execution';
  states: Record<string, any>;
  context: any;
}

export type MacroExecutionState = any;
export type MacroExecutionEvent = any;